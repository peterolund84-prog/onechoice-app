export function PlaceholderPage({ title }: { title: string }) {
  return (
    <section className="oc-placeholder">
      <h1>{title}</h1>
      <p>Flyttas hit från Streamlit i nästa steg.</p>
    </section>
  );
}
